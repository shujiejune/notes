---
import { formatDateToISO } from "../utils/formatDate.js";

interface Props {
  date: Date;
}

const { date } = Astro.props;
---

<time datetime={formatDateToISO(date)}>{formatDateToISO(date)}</time>
